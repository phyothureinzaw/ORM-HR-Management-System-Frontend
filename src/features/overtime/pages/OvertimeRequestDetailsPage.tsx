import { useState } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { toast } from 'sonner'
import { Button } from '../../../components/ui/Button'
import { Card, CardContent } from '../../../components/ui/Card'
import { PageHeader } from '../../../components/common/PageHeader'
import { PermissionGuard } from '../../auth/components/PermissionGuard'
import { UnauthorizedPage } from '../../../pages/UnauthorizedPage'
import { Permissions } from '../../../lib/permissions'
import { usePermission } from '../../auth/hooks/usePermission'
import { useMyOvertimeRequest, useOvertimeRequestMutations } from '../hooks/useOvertimeRequests'
import { OvertimeNavigation } from '../components/OvertimeNavigation'
import { approvalStatusLabels, approverTypeLabels, dateText, dayCategoryLabels, durationText, localRange, requestStatusLabels, segmentLabels, statusClass } from '../utils/overtimeRequestFormatters'
function Detail({ label, value }: { label: string; value: string }) { return <div className="overtime-detail-item"><small>{label}</small><strong>{value}</strong></div> }
export function OvertimeRequestDetailsPage() {
  const { overtimeRequestId } = useParams(); const query = useMyOvertimeRequest(overtimeRequestId); const mutations = useOvertimeRequestMutations(); const navigate = useNavigate(); const [reason, setReason] = useState(''); const canRequest = usePermission(Permissions.Overtime.Request)
  if (query.fetchStatus === 'fetching') return <main className="overtime-page"><div className="overtime-loading" role="status">Loading request details...</div></main>
  if (query.isError || !query.data) return <PermissionGuard permission={Permissions.Overtime.View} fallback={<UnauthorizedPage />}><main className="overtime-page"><div className="overtime-alert" role="alert">Request details could not be loaded. <Link className="text-link" to="/overtime">Back to My Overtime</Link></div></main></PermissionGuard>
  const details = canRequest ? query.data : { ...query.data, canEdit: false, canCancel: false }; const request = details.request
  async function cancel() { if (!window.confirm(`Cancel ${request.requestNumber ?? 'this request'}?`)) return; try { await mutations.cancel.mutateAsync({ id: request.id, request: { cancellationReason: reason.trim() || null, rowVersion: request.rowVersion } }); toast.success('Overtime request cancelled.'); void query.refetch() } catch (cause) { toast.error(cause instanceof Error ? cause.message : 'Cancellation is no longer available.') } }
  return <PermissionGuard permission={Permissions.Overtime.View} fallback={<UnauthorizedPage />}><main className="overtime-page"><OvertimeNavigation /><PageHeader eyebrow="Overtime request" title={request.requestNumber ?? 'Request details'} description="Review request information, relationships, and approval progress." /><div className="overtime-detail-actions"><Link className="text-link" to="/overtime">Back to My Overtime</Link><div className="action-row">{details.canEdit && <Button variant="secondary" onClick={() => navigate(`/overtime/requests/${request.id}/edit`)}>Edit request</Button>}{details.canCancel && <><input aria-label="Cancellation reason" placeholder="Cancellation reason (optional)" value={reason} onChange={(event) => setReason(event.target.value)} /><Button variant="ghost" disabled={mutations.cancel.isPending} onClick={() => void cancel()}>Cancel request</Button></>}</div></div><Card><CardContent><div className="overtime-detail-grid"><Detail label="Status" value={requestStatusLabels[request.status]} /><Detail label="Overtime type" value={request.type.name} /><Detail label="Project" value={request.projectNameSnapshot ?? '—'} /><Detail label="Day category" value={dayCategoryLabels[request.dayCategory]} /><Detail label="Segment" value={segmentLabels[request.segmentType]} /><Detail label="Local date/time" value={localRange(request.startDate, request.startTime, request.endDate, request.endTime)} /><Detail label="Requested duration" value={durationText(request.requestedMinutes)} /><Detail label="Approved duration" value={durationText(request.approvedMinutes)} /><Detail label="Submitted" value={dateText(request.submittedAtUtc.slice(0, 10))} /><Detail label="Cancellation reason" value={details.cancellationReason ?? '—'} /><div className="overtime-detail-wide"><Detail label="Reason" value={details.reason} /><Detail label="Work description" value={details.workDescription ?? '—'} /></div></div></CardContent></Card><div className="overtime-related-grid">{details.parent && <Card><CardContent><h2>Linked evening request</h2><p>{details.parent.requestNumber ?? details.parent.id}</p><p>{localRange(details.parent.startDate, details.parent.startTime, details.parent.endDate, details.parent.endTime)} · {details.parent.projectNameSnapshot ?? 'No project'}</p><p className="overtime-note">Morning continuation uses the same project and remains linked to this request.</p></CardContent></Card>}{details.continuations.length > 0 && <Card><CardContent><h2>Continuation requests</h2>{details.continuations.map((item) => <p key={item.id}><Link className="text-link" to={`/overtime/requests/${item.id}`}>{item.requestNumber ?? item.id}</Link> · {localRange(item.startDate, item.startTime, item.endDate, item.endTime)}</p>)}</CardContent></Card>}</div><Card><CardContent><h2>Approval timeline</h2><div className="overtime-timeline">{details.approvals.length === 0 ? <p>No approval activity yet.</p> : details.approvals.map((approval) => <div key={approval.id}><strong>Level {approval.levelNumber} · {approverTypeLabels[approval.approverType]}</strong><span className={statusClass(approval.status)}>{approvalStatusLabels[approval.status]}</span><p>{approval.comment ?? 'No comment'}</p></div>)}</div></CardContent></Card></main></PermissionGuard>
}
