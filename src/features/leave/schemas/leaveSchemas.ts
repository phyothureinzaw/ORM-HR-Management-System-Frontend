import { z } from 'zod'
const days = z.number().min(0).max(366).refine((value) => Number.isInteger(value * 100), 'Use at most two decimal places.')
export const leaveTypeSchema = z.object({ code: z.string().trim().min(1).max(30).regex(/^[A-Za-z0-9_-]+$/, 'Use letters, numbers, hyphens, or underscores.'), name: z.string().trim().min(1).max(150), description: z.string().max(500), defaultDaysPerYear: days, isPaid: z.boolean(), allowHalfDay: z.boolean(), allowNegativeBalance: z.boolean(), requiresAttachment: z.boolean(), isActive: z.boolean() })
export type LeaveTypeFormValues = z.infer<typeof leaveTypeSchema>
export const leaveBalanceSchema = z.object({ employeeId: z.string().min(1, 'Select an employee.'), leaveTypeId: z.string().min(1, 'Select a Leave Type.'), balanceYear: z.number().int().min(2000).max(2100), entitledDays: days, carriedForwardDays: days, adjustedDays: z.number().min(-366).max(366).refine((value) => Number.isInteger(value * 100), 'Use at most two decimal places.') })
export type LeaveBalanceFormValues = z.infer<typeof leaveBalanceSchema>
export const approvalLevelSchema = z.object({ levelNumber: z.number().int().min(1).max(5), name: z.string().trim().min(1).max(100), approverType: z.union([z.literal(1), z.literal(2), z.literal(3)]), approverRoleId: z.string().nullable(), approverEmployeeId: z.string().nullable(), isActive: z.boolean() }).superRefine((value, ctx) => { if (value.approverType === 2 && !value.approverRoleId) ctx.addIssue({ code: 'custom', path: ['approverRoleId'], message: 'Select a role.' }); if (value.approverType === 3 && !value.approverEmployeeId) ctx.addIssue({ code: 'custom', path: ['approverEmployeeId'], message: 'Select an employee.' }); if (value.approverType === 1 && (value.approverRoleId || value.approverEmployeeId)) ctx.addIssue({ code: 'custom', path: ['approverType'], message: 'Manager approval does not use an assignment.' }) })
export const approvalSettingSchema = z.object({ isEnabled: z.boolean(), requiredLevels: z.number().int().min(1).max(5), allowPendingCancellation: z.boolean() })
