import { useState } from 'react'; import { Link, useNavigate, useParams } from 'react-router-dom'; import { Button } from '../../../components/ui/Button'; import { Card, CardContent } from '../../../components/ui/Card'; import { PageHeader } from '../../../components/common/PageHeader'; import { LeaveApprovalTimeline } from '../components/LeaveApprovalTimeline'; import { LeaveRequestSummary } from '../components/LeaveRequestSummary'; import { useLeaveRequest } from '../hooks/useLeaveRequest'; import { useLeaveRequestMutations } from '../hooks/useLeaveRequestMutations'; import { toast } from 'sonner'
export function LeaveRequestDetailsPage() { const { leaveRequestId } = useParams(); const query = useLeaveRequest(leaveRequestId); const mutations = useLeaveRequestMutations(leaveRequestId); const navigate = useNavigate(); const [reason, setReason] = useState(''); const [comment, setComment] = useState(''); if (query.isPending) return <p role="status">Loading request details…</p>; if (query.isError || !query.data) return <div role="alert"><h1>Leave request not found</h1><Link to="/leave">Back to My Leave</Link></div>; const details = query.data; return <><PageHeader eyebrow="Leave request" title={details.request.requestNumber ?? 'Request details'} description="Review request information and approval progress." /><Card><CardContent><LeaveRequestSummary details={details} /><div className="action-row">{details.canEdit && <Button variant="secondary" onClick={() => navigate(`/leave/${details.request.id}/edit`)}>Edit request</Button>}{details.canCancel && <><input aria-label="Cancellation reason" value={reason} maxLength={500} onChange={(event) => setReason(event.target.value)} placeholder="Optional cancellation reason" /><Button variant="ghost" disabled={mutations.cancel.isPending} onClick={() => { if (window.confirm(`Cancel ${details.request.requestNumber ?? 'this request'}? This action cannot be undone.`)) mutations.cancel.mutate({ id: details.request.id, reason }, { onSuccess: () => toast.success('Leave request cancelled.'), onError: () => toast.error('Cancellation is no longer available.') }) }}>Cancel request</Button></>}{details.canApprove && <Button disabled={mutations.approve.isPending} onClick={() => mutations.approve.mutate({ id: details.request.id, comment }, { onSuccess: () => toast.success('Leave request approved.'), onError: () => toast.error('This request is no longer actionable.') })}>Approve request</Button>}{details.canDecline && <><input aria-label="Decline comment" required maxLength={1000} value={comment} onChange={(event) => setComment(event.target.value)} placeholder="Decline comment (required)" /><Button variant="ghost" disabled={!comment.trim() || mutations.decline.isPending} onClick={() => mutations.decline.mutate({ id: details.request.id, comment }, { onSuccess: () => toast.success('Leave request declined.'), onError: () => toast.error('This request is no longer actionable.') })}>Decline request</Button></>}</div></CardContent></Card><Card><CardContent><h2>Approval progress</h2><LeaveApprovalTimeline approvals={details.approvals} current={details.request.currentApprovalLevel} /></CardContent></Card></> }
