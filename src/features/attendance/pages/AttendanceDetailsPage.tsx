import { ArrowLeft, CircleAlert, MapPin } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { Card, CardContent } from '../../../components/ui/Card'
import { PageHeader as AttendancePageHeader } from '../../../components/common/PageHeader'
import { PermissionGuard } from '../../auth/components/PermissionGuard'
import { Permissions } from '../../../lib/permissions'
import { UnauthorizedPage } from '../../../pages/UnauthorizedPage'
import { AttendanceStatusBadge } from '../components/AttendanceStatusBadge'
import { ErrorState, LoadingState } from '../components/AttendanceShared'
import { useMyAttendanceDetails } from '../hooks/useAttendanceEmployee'
import { formatDateOnly, formatDuration, formatUtc, sourceLabel } from '../utils/attendanceFormatters'

export function AttendanceDetailsPage() { const { attendanceRecordId } = useParams(); const query = useMyAttendanceDetails(attendanceRecordId); return <PermissionGuard permission={Permissions.Attendance.View} fallback={<UnauthorizedPage />}><div className="attendance-page employee-attendance-page"><AttendancePageHeader title="Attendance details" description="Review the details recorded for this attendance event." action={<Link className="text-link" to="/attendance/history"><ArrowLeft size={15} /> Back to My Attendance</Link>} />{query.isPending ? <LoadingState label="Loading Attendance details" /> : query.isError ? <ErrorState title="Attendance details could not be loaded" error={query.error} retry={() => void query.refetch()} /> : !query.data ? <div className="alert alert-error"><CircleAlert size={17} />This attendance record was not found.</div> : <div className="attendance-detail-grid"><Card><CardContent><div className="attendance-detail-header"><div><span className="attendance-local-date">{formatDateOnly(query.data.attendanceDate)}</span><h2>{query.data.shift?.code ?? 'Attendance event'}</h2><p>{query.data.shift?.name ?? 'No shift recorded'}</p></div><AttendanceStatusBadge status={query.data.status} /></div><dl className="attendance-detail-list"><Detail label="Source" value={sourceLabel(query.data.source)} /><Detail label="Scheduled start" value={formatUtc(query.data.scheduledStartDateTimeUtc)} /><Detail label="Scheduled end" value={formatUtc(query.data.scheduledEndDateTimeUtc)} /><Detail label="Check-In" value={formatUtc(query.data.checkInDateTimeUtc)} /><Detail label="Check-Out" value={formatUtc(query.data.checkOutDateTimeUtc)} /><Detail label="Late" value={query.data.lateMinutes ? `${query.data.lateMinutes} minutes` : 'On time'} /><Detail label="Early leave" value={query.data.checkOutDateTimeUtc ? `${query.data.earlyLeaveMinutes} minutes` : 'Not checked out'} /><Detail label="Work duration" value={query.data.checkOutDateTimeUtc ? formatDuration(query.data.workMinutes) : 'Not completed'} /></dl></CardContent></Card><Card><CardContent><h2 className="attendance-detail-section-title">Location and remarks</h2><dl className="attendance-detail-list"><Detail label="Check-in location" value={query.data.checkInLocation?.name ?? 'Not recorded'} icon={query.data.isCheckInOutsideLocation ? <span className="attendance-accent">Outside approved radius</span> : undefined} /><Detail label="Check-out location" value={query.data.checkOutLocation?.name ?? 'Not recorded'} icon={query.data.isCheckOutOutsideLocation ? <span className="attendance-accent">Outside approved radius</span> : undefined} /><Detail label="Check-in remark" value={query.data.checkInRemark ?? '—'} /><Detail label="Check-out remark" value={query.data.checkOutRemark ?? '—'} /><Detail label="Outside reason" value={query.data.outsideCheckInReason || query.data.outsideCheckOutReason || '—'} /><Detail label="Adjusted" value={query.data.isManuallyAdjusted ? 'Yes' : 'No'} /></dl><p className="attendance-detail-note"><MapPin size={14} /> Location status is calculated by the Attendance server.</p></CardContent></Card></div>}</div></PermissionGuard> }
function Detail({ label, value, icon }: { label: string; value: string; icon?: React.ReactNode }) { return <div><dt>{label}</dt><dd>{value}{icon && <small>{icon}</small>}</dd></div> }
